# Django settings
DJANGO_SECRET_KEY=
DJANGO_DEBUG=True
# Database configuration (SQLite default or use a DATABASE_URL for other DBs)
DATABASE_URL=sqlite:///db.sqlite3
# Optional: Override OSRM routing service URL
OSRM_URL=http://router.project-osrm.org/route/v1/driving/
# Allowed hosts (comma-separated)
ALLOWED_HOSTS=localhost,127.0.0.1
